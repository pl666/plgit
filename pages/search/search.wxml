<view class="search">
<input bindinput='input'></input>
<button bindtap='search'>搜索</button>
</view>

<!-- 渲染查询列表 -->
<view wx:for="{{list}}" wx:key="{{item.id}}">
  <view bindtap='go'>
   {{item.id}} {{item.inputdec}} {{item.inputconcat}}
  </view> 
</view>